name: Fix AOG match parity

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply parity patch
        run: python3 tools/fix_aog_match_parity.py
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/Aog/Dispatcher.php src/Aog/MiscDispatcher.php
          git diff --cached --quiet && exit 0
          git commit -m 'Match original AOG boot and matching contracts'
          git pull --rebase origin main
          git push
